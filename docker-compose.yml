version: "3"

services: 
  db: 
    image: postgres
    container_name: todo_container
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: todo
    ports: 
      - "5432:5432"
